/*
 * let
 *
 *   let 在很多方面与var是相似的，
 *   但是可以帮助大家避免在 JavaScript 里常见一些问题。
 *
 *      1. 避免声明提前
 *      2. 提供块作用域
 *      3. 禁止变量的重定义
 */
let num: number = 1;

/*
 * const
 *
 *   const 是对 let 的一个增强，它能阻止对一个变量再次赋值。
 */
const PI: number = 3.14;
