<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fn.sb.hello.sys.mapper.SysUserMapper">

    <select id="queryUserList" resultType="SysUser">
        select * from sys_user
    </select>
    <!--
    <select id="queryUserById" parameterType="User">
        select * from user where user_id = #{userId}
    </select>

    <insert id="addUser" parameterType="User">
        insert into user(user_id, user_name, login_name, login_pwd, is_enable)
                    values (#{userId},#{userName},#{loginName},#{loginPwd},#{isEnable})
    </insert>

    <update id="updateUser" parameterType="User">
        update user set user_name = #{userName} where user_id = #{userId}
    </update>

    <delete id="deleteUser" parameterType="string">
        delete from user where user_id = #{userId}
    </delete>-->
</mapper>